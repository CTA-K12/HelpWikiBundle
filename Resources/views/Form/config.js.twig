mesdConfig = {
    language:         'en',
    entities:         false,
    htmlEncodeOutput: false,
    protectedSource:  [],
    extraPlugins:     'twig' // Twig Insert Plugin
};

mesdConfig.protectedSource.push({{ '/\{\{\s.+\s\}\}/g'|raw }});
mesdConfig.protectedSource.push({{ '/\{%\s.+\s%\}/g'|raw }});

CKEDITOR.replace('{{ id }}', mesdConfig);
CKEDITOR.instances['{{ id }}'].rawConfig = mesdConfig;
